#include <stdio.h>
int main (){
    int unoNum = 3;
    int segNum = 4;
    int terNum = 3;
    int mult=unoNum*segNum*terNum;
    printf (" La multiplicacion de %d * %d * %d = %d\n", unoNum, segNum, terNum, mult);
    printf ("\n");
    return 0;
    
}
